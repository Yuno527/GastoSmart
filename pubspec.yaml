name: proyecto_movil
description: Proyecto movil base.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Estado + DI simple
  flutter_riverpod: ^2.5.1

  # Routing
  go_router: ^14.2.0

  # Utils
  intl: ^0.19.0

  # Persistencia local
  shared_preferences: ^2.3.2

  supabase_flutter: ^2.0.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
